services:
  db:
    image: postgres:16-alpine
    container_name: ventura-db
    environment:
      POSTGRES_DB: ventura
      POSTGRES_USER: ventura
      POSTGRES_PASSWORD: ventura
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
